import 'package:flutter/material.dart';

class RegistroMantenimientoScreen extends StatelessWidget {
  const RegistroMantenimientoScreen({super.key});
  
  @override
  Widget build(BuildContext context) {
    return Scaffold(
    appBar: AppBar(title: const Text('Registro mantenimiento')),
    body: const Center(child: Text('Pagina de resgistro')),
  );
  }
}
