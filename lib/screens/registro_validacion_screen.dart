import 'package:flutter/material.dart';

class RegistroValidacionScreen extends StatelessWidget {
  const RegistroValidacionScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Registro de vaalidación')),
      body: const Center(child: Text('Pagina de registro de validaciones')),
    );
  }
}
